<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color de fondo con transparencia -->
    <corners android:radius="8dp" /> <!-- Esquinas redondeadas -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp" /> <!-- Espaciado interno -->

    <!-- Efecto de desenfoque utilizando el atributo 'android:foreground' -->
    <item android:drawable="@drawable/glassmorphism_blur" />
</shape>